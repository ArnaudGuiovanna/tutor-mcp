# Regulation design archive

> **Historical design record — non-normative.** The documents in this
> directory preserve the design and migration reasoning that led to the
> current regulation pipeline. References to `engine/router.go`,
> `engine.Route`, or `REGULATION_PHASE` describe removed transitional
> mechanisms and are not current runtime instructions.
>
> The supported implementation is the always-on orchestration pipeline in
> `engine/orchestrator.go`. For current product behaviour and deployment
> guidance, use the source code, MCP tool schemas, `README.md`, and
> `OPERATIONS.md`.
